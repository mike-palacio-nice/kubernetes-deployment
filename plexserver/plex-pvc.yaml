apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plex-pvc
  namespace: plexserver
spec:
  storageClassName: NFS-PlexMedia
  accessModes: [ReadWriteMany]
  resources:
    requests:
      storage: 2Ti
