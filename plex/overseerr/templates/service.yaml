apiVersion: v1
kind: Service
metadata:
  name: {{ template "overseerr.fullname" . }}
  labels:
    app: {{ template "overseerr.name" . }}
    chart: {{ template "overseerr.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
      nodePort: {{ .Values.service.nodePort }}
  selector:
    app: {{ template "overseerr.name" . }}
    release: {{ .Release.Name }}
